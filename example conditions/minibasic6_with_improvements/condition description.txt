# Overview

This condition takes the same inputs as the `minibasic6` condition, but enables additional features of the framework to produce a better guessing model.  It also uses a lower probability cutoff to evaluate a larger number of guesses.

